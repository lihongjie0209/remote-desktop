[package]
name = "proto"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic  = "0.12"
prost  = "0.13"

[build-dependencies]
tonic-build = "0.12"
# Vendored protoc binary — works on all platforms including Docker/cross builds.
protoc-bin-vendored = "3"
