name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # ──────────────────────────────────────────────────────────
  # Server: Linux (x86_64 + aarch64) + Windows + macOS
  # ──────────────────────────────────────────────────────────
  build-server:
    name: Server (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-22.04
            archive: tar.gz
          - target: aarch64-unknown-linux-musl
            os: ubuntu-22.04
            archive: tar.gz
            cross: true
          - target: x86_64-pc-windows-msvc
            os: windows-2022
            archive: zip
          - target: x86_64-apple-darwin
            os: macos-13
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-14
            archive: tar.gz

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install musl tools (Linux)
        if: contains(matrix.target, 'linux')
        run: sudo apt-get install -y musl-tools

      - name: Install cross (aarch64)
        if: matrix.cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build server
        shell: bash
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build -p server --release --target ${{ matrix.target }}
          else
            cargo build -p server --release --target ${{ matrix.target }}
          fi

      - name: Package (Unix)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          BIN=target/${{ matrix.target }}/release/server
          ASSET=server-${{ github.ref_name }}-${{ matrix.target }}.tar.gz
          tar czf "$ASSET" -C "$(dirname $BIN)" "$(basename $BIN)"
          echo "ASSET=$ASSET" >> $GITHUB_ENV

      - name: Package (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $bin = "target\${{ matrix.target }}\release\server.exe"
          $asset = "server-${{ github.ref_name }}-${{ matrix.target }}.zip"
          Compress-Archive -Path $bin -DestinationPath $asset
          echo "ASSET=$asset" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ASSET }}

  # ──────────────────────────────────────────────────────────
  # Desktop: Windows
  # ──────────────────────────────────────────────────────────
  build-desktop-windows:
    name: Desktop (Windows x64)
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install NASM
        run: |
          $url = "https://www.nasm.us/pub/nasm/releasebuilds/2.16.03/win64/nasm-2.16.03-win64.zip"
          Invoke-WebRequest -Uri $url -OutFile nasm.zip
          Expand-Archive nasm.zip -DestinationPath C:\nasm
          echo "C:\nasm\nasm-2.16.03" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Install frontend deps
        working-directory: apps/desktop
        run: npm ci

      - name: Build desktop
        working-directory: apps/desktop
        env:
          CMAKE_POLICY_VERSION_MINIMUM: "3.5"
        run: npm run tauri build

      - name: Rename artifacts
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $msi = Get-ChildItem target/release/bundle/msi/*.msi | Select-Object -First 1
          if ($msi) {
            $dest = "target/release/bundle/msi/RemoteDesk-$tag-windows-x64.msi"
            Move-Item $msi.FullName $dest
          }
          $nsis = Get-ChildItem target/release/bundle/nsis/*-setup.exe | Select-Object -First 1
          if ($nsis) {
            $dest = "target/release/bundle/nsis/RemoteDesk-$tag-windows-x64-setup.exe"
            Move-Item $nsis.FullName $dest
          }

      - name: Upload MSI + NSIS
        uses: softprops/action-gh-release@v2
        with:
          files: |
            target/release/bundle/msi/RemoteDesk-${{ github.ref_name }}-windows-x64.msi
            target/release/bundle/nsis/RemoteDesk-${{ github.ref_name }}-windows-x64-setup.exe

  # ──────────────────────────────────────────────────────────
  # Desktop: macOS (Intel + Apple Silicon)
  # ──────────────────────────────────────────────────────────
  build-desktop-macos:
    name: Desktop (macOS ${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            os: macos-13
            target: x86_64-apple-darwin
          - arch: aarch64
            os: macos-14
            target: aarch64-apple-darwin

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install frontend deps
        working-directory: apps/desktop
        run: npm ci

      - name: Build desktop
        working-directory: apps/desktop
        run: npm run tauri build -- --target ${{ matrix.target }}

      - name: Rename artifacts
        shell: bash
        run: |
          TAG="${{ github.ref_name }}"
          ARCH="${{ matrix.arch }}"
          DMG=$(find target/${{ matrix.target }}/release/bundle/dmg -name "*.dmg" | head -1)
          if [ -n "$DMG" ]; then
            DIR=$(dirname "$DMG")
            mv "$DMG" "$DIR/RemoteDesk-${TAG}-macos-${ARCH}.dmg"
          fi

      - name: Upload DMG
        uses: softprops/action-gh-release@v2
        with:
          files: target/${{ matrix.target }}/release/bundle/dmg/RemoteDesk-${{ github.ref_name }}-macos-${{ matrix.arch }}.dmg

  # ──────────────────────────────────────────────────────────
  # Desktop: Linux
  # ──────────────────────────────────────────────────────────
  build-desktop-linux:
    name: Desktop (Linux x64)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev \
            librsvg2-dev patchelf nasm libxdo-dev libturbojpeg0-dev

      - name: Install frontend deps
        working-directory: apps/desktop
        run: npm ci

      - name: Build desktop
        working-directory: apps/desktop
        env:
          CMAKE_POLICY_VERSION_MINIMUM: "3.5"
        run: npm run tauri build

      - name: Rename artifacts
        shell: bash
        run: |
          TAG="${{ github.ref_name }}"
          APP=$(find target/release/bundle/appimage -name "*.AppImage" | head -1)
          if [ -n "$APP" ]; then
            mv "$APP" "target/release/bundle/appimage/RemoteDesk-${TAG}-linux-x64.AppImage"
          fi
          DEB=$(find target/release/bundle/deb -name "*.deb" | head -1)
          if [ -n "$DEB" ]; then
            mv "$DEB" "target/release/bundle/deb/RemoteDesk-${TAG}-linux-x64.deb"
          fi

      - name: Upload AppImage + deb
        uses: softprops/action-gh-release@v2
        with:
          files: |
            target/release/bundle/appimage/RemoteDesk-${{ github.ref_name }}-linux-x64.AppImage
            target/release/bundle/deb/RemoteDesk-${{ github.ref_name }}-linux-x64.deb

